{
  "name": "sisyphus-platform",
  "version": "0.1.0",
  "private": true,
  "description": "AI-native VSCode platform (kernel + plugins). See concepts/sisyphus-plugin-architecture.md in wiki.",
  "scripts": {
    "dev": "pnpm --filter @sisyphus/ui dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r --parallel typecheck",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.2"
  }
}
