{
  "name": "@sisyphus/daemon",
  "version": "0.1.0",
  "private": true,
  "description": "Sisyphus daemon — Node process hosting plugins and serving HTTP+WS API (M1+).",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@sisyphus/kernel": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20",
    "tsx": "^4.19.0",
    "typescript": "^5"
  }
}
